# fast_read

Simple MVP: upload PDF, read one word at a time at `100 WPM` by default, then submit feedback.

## Run

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Open: `http://127.0.0.1:5000`
